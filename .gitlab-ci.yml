image: kennethreitz/pipenv

stages:
  - deploy

pages:
  stage: deploy
  script:
    - pipenv sync
    - pipenv run mkdocs build --strict --verbose --site-dir public
  artifacts:
    paths:
    - public
  only:
    - master
